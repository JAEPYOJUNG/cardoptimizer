<!DOCTYPE html>
<html>
  <head>
    <% include ../../partials/head %>


    <meta charset="utf-8"/>
    <meta name="viewport" content="user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0, width=device-width"/>

<script src="//developers.kakao.com/sdk/js/kakao.min.js"></script>
<script> Kakao.init('f1643921732f418af4cbdf08e8f6fde4'); </script>
  </head>
  <body>
    <% include ../../partials/nav %>

    <div class="container home home-welcome">
      <div class="jumbotron">
        <h1>공지사항</h1>
        <P>이 사이트는 신용카드 사용의 도움을 주기 위해 만들어졌습니다.</p>

        <p id="userinfo"></p>

        <a id="kakao-login-btn"></a>

        <p id="uname"></p>
        <img src="" id="uphoto"/>



      </div>
    </div>



    <script type='text/javascript'>
         // 카카오 로그인 버튼을 생성합니다.
        Kakao.Auth.createLoginButton({
          container: '#kakao-login-btn',
          success: function(authObj) {
            // 로그인 성공시, API를 호출합니다.
            Kakao.API.request({
              url: '/v2/user/me',
              success: function(res) {
                // alert(response_str);
                var response_str = JSON.stringify(res);
                // document.getElementById("userinfo").innerHTML = response_str;

                var parsed_obj = JSON.parse(response_str);
                document.getElementById("uname").innerHTML = parsed_obj.properties.nickname;
                document.getElementById("uphoto").src = parsed_obj.properties.thumbnail_image;


              },
              fail: function(error) {
                alert(JSON.stringify(error));
              }
            });
          },
          fail: function(err) {
            alert(JSON.stringify(err));
          }
        });
    </script>




  </body>
</html>
